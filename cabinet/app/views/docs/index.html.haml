.wrapper_with_padding
  #docs.clearfix
  -# if the user has documents
  - unless @docs.blank?
    -# render info and link for all docs
    - @docs.each do |doc|
      -# Creates a div with the class of doc
      %a{href: (url_for [doc])}
        .doc 
          -# creates a link to view doc
          %p.title= doc.title
          -# how long ago it was created
          %p.date= time_ago_in_words(doc.created_at)
          -# small snippet from body
          %p.content= truncate(doc.content, length:50)
  - else
    %h2 Create Doc!
    %p Start Creating Documents and organizing your life
    %button= link_to "Create Doc", new_doc_path 
    